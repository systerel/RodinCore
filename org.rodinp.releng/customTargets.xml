<project name="Build specific targets and properties" default="noDefault">

	<!-- ===================================================================== -->
	<!-- Run a given ${target} on all elements being built -->
	<!-- Add on <ant> task for each top level element being built. -->
	<!-- ===================================================================== -->
	<available property="allElementsFile" file="${builder}/allElements.xml" value="${builder}/allElements.xml"/>
	<property name="allElementsFile" location="${eclipse.pdebuild.templates}/headless-build/allElements.xml"/>

	<import file="${allElementsFile}" />
	<target name="allElements">
		<antcall target="allElementsDelegator" />
	</target>
	
	<!-- ===================================================================== -->
	<!-- ===================================================================== -->
	<target name="getBaseComponents" depends="checkLocalBase" unless="skipBase">
		<get src="${eclipseBaseURL}" dest="${buildDirectory}/../temp-base.zip" />
		<unzip dest="${base}" overwrite="true" src="${buildDirectory}/../temp-base.zip" />
	</target>

	<target name="checkLocalBase">
		<available file="${base}" property="skipBase" />
	</target>

	<!-- ===================================================================== -->
	<!-- Check out map files from correct repository -->
	<!-- Replace values for mapsCheckoutTag as desired. -->
	<!-- ===================================================================== -->
	<target name="getMapFiles" depends="checkLocalMaps" unless="skipMaps">
		<property name="mapsCheckoutTag" value="HEAD" />
		<cvs cvsRoot="${mapsRepo}" package="${mapsRoot}" dest="${buildDirectory}/maps" tag="${mapsCheckoutTag}" />
	</target>

	<target name="checkLocalMaps">
		<available property="skipMaps" file="${buildDirectory}/maps" />
	</target>

	<target name="tagMapFiles" if="tagMaps">
		<cvs dest="${buildDirectory}/maps/${mapsRoot}" command="tag ${mapsTagTag}" />
	</target>

	<!-- ===================================================================== -->

	<target name="clean" unless="noclean">
		<antcall target="allElements">
			<param name="target" value="cleanElement" />
		</antcall>
	</target>

	<target name="gatherLogs">
		<mkdir dir="${buildDirectory}/${buildLabel}/compilelogs" />
		<antcall target="allElements">
			<param name="target" value="gatherLogs" />
		</antcall>
		<unzip dest="${buildDirectory}/${buildLabel}/compilelogs" overwrite="true">
			<fileset dir="${buildDirectory}/features">
				<include name="**/*.log.zip" />
			</fileset>
		</unzip>
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do before setup -->
	<!-- ===================================================================== -->
	<target name="preSetup">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after setup but before starting the build proper -->
	<!-- ===================================================================== -->
	<target name="postSetup">
		<antcall target="getBaseComponents" />
        <antcall target="copySources" />
        <antcall target="fixVersions" />
	</target>

	<!-- Skipped when run through CruiseControl, as CC uses a different mechanism -->
	<target name="copySources" unless="cruisecontrol">
	      <!-- Copy feature and plug-in sources -->
	        <mkdir dir="${buildDirectory}/features" />
	        <copy todir="${buildDirectory}/features/org.rodinp">
	            <fileset dir="${builder}/../org.rodinp-feature" />
	        </copy>
	        <copy todir="${buildDirectory}/features/org.rodinp.platform">
	            <fileset dir="${builder}/../org.rodinp.platform-feature" />
	        </copy>
	        <copy todir="${buildDirectory}/features/org.eventb.ide">
	            <fileset dir="${builder}/../org.eventb.ide-feature" />
	        </copy>

	        <mkdir dir="${buildDirectory}/plugins" />
	        <copy todir="${buildDirectory}/plugins/org.rodinp">
	            <fileset dir="${builder}/../org.rodinp" />
	        </copy>
	        <copy todir="${buildDirectory}/plugins/org.rodinp.core">
	            <fileset dir="${builder}/../org.rodinp.core" />
	        </copy>
	        <copy todir="${buildDirectory}/plugins/org.rodinp.keyboard">
	            <fileset dir="${builder}/../org.rodinp.keyboard" />
	        </copy>

	        <copy todir="${buildDirectory}/plugins/org.rodinp.platform">
	            <fileset dir="${builder}/../org.rodinp.platform" />
	        </copy>

	        <copy todir="${buildDirectory}/plugins/org.eventb.core">
	            <fileset dir="${builder}/../org.eventb.core" />
	        </copy>
	        <copy todir="${buildDirectory}/plugins/org.eventb.core.ast">
	            <fileset dir="${builder}/../org.eventb.core.ast" />
	        </copy>
	        <copy todir="${buildDirectory}/plugins/org.eventb.core.seqprover">
	            <fileset dir="${builder}/../org.eventb.core.seqprover" />
	        </copy>
	        <copy todir="${buildDirectory}/plugins/org.eventb.doc.user">
	            <fileset dir="${builder}/../org.eventb.doc.user" />
	        </copy>
	        <copy todir="${buildDirectory}/plugins/org.eventb.keyboard">
	            <fileset dir="${builder}/../org.eventb.keyboard" />
	        </copy>
	        <copy todir="${buildDirectory}/plugins/ch.ethz.eventb.keyboard.latex">
    	        <fileset dir="${builder}/../ch.ethz.eventb.keyboard.latex" />
        	</copy>
	        <copy todir="${buildDirectory}/plugins/org.eventb.ui">
	            <fileset dir="${builder}/../org.eventb.ui" />
	        </copy>
	        <copy todir="${buildDirectory}/plugins/org.eventb.ui.symboltable">
    	        <fileset dir="${builder}/../org.eventb.ui.symboltable" />
        	</copy>
	        <copy todir="${buildDirectory}/plugins/org.eventb.ide">
	            <fileset dir="${builder}/../org.eventb.ide" />
	        </copy>
	        <copy todir="${buildDirectory}/plugins/org.eventb.pp">
	            <fileset dir="${builder}/../org.eventb.pp" />
	        </copy>
	        <copy todir="${buildDirectory}/plugins/org.eventb.pptrans">
	            <fileset dir="${builder}/../org.eventb.pptrans" />
	        </copy>
	        <copy todir="${buildDirectory}/plugins/org.eventb.pp.ui">
	            <fileset dir="${builder}/../org.eventb.pp.ui" />
	        </copy>
	        <copy todir="${buildDirectory}/plugins/fr.systerel.explorer">
	            <fileset dir="${builder}/../fr.systerel.explorer" />
	        </copy>

	</target>

    <!-- Fix product version -->
	<target name="fixVersions"
		description="Fix product name and artefact versions">
		<replace
			file="${buildDirectory}/plugins/org.rodinp.platform/Rodin.product"
			summary="true">
			<replacefilter token="%productName" value="Rodin Platform"/>
			<replacefilter token="@version@" value="${buildVersion}"/>
		</replace>
		<replace
			file="${buildDirectory}/plugins/org.rodinp.platform/plugin.properties"
			summary="true">
			<replacefilter token="@version@" value="${buildId}"/>
		</replace>
	</target>

	
	<!-- ===================================================================== -->
	<!-- Steps to do before fetching the build elements -->
	<!-- ===================================================================== -->
	<target name="preFetch">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after fetching the build elements -->
	<!-- ===================================================================== -->
	<target name="postFetch">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do before the repositories are being processed -->
	<!-- ===================================================================== -->
	<target name="preProcessRepos">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after the repositories have been processed -->
	<!-- ===================================================================== -->
	<target name="postProcessRepos">
	</target>
	
	<!-- ===================================================================== -->
	<!-- Steps to do before generating the build scripts. -->
	<!-- ===================================================================== -->
	<target name="preGenerate">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after generating the build scripts. -->
	<!-- ===================================================================== -->
	<target name="postGenerate">
		<antcall target="clean" />
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do before running the build.xmls for the elements being built. -->
	<!-- ===================================================================== -->
	<target name="preProcess">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after running the build.xmls for the elements being built. -->
	<!-- ===================================================================== -->
	<target name="postProcess">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do before running assemble. -->
	<!-- ===================================================================== -->
	<target name="preAssemble">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after  running assemble. -->
	<!-- ===================================================================== -->
	<target name="postAssemble">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do before running package. -->
	<!-- ===================================================================== -->
	<target name="prePackage">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after  running package. -->
	<!-- ===================================================================== -->
	<target name="postPackage">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after the build is done. -->
	<!-- ===================================================================== -->
	<target name="postBuild">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do to test the build results -->
	<!-- ===================================================================== -->
	<target name="test">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do to publish the build results -->
	<!-- ===================================================================== -->
	<target name="publish">
	</target>

	<!-- ===================================================================== -->
	<!-- Default target                                                        -->
	<!-- ===================================================================== -->
	<target name="noDefault">
		<echo message="You must specify a target when invoking this file" />
	</target>

</project>
